# Local Environment Variables
# Copy from .env.example and populate with your own keys when ready.
# Without these, the application operates in "Demo Mode" using seed mock data.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
